module.exports = `
Commands:

yoltools create 
Launch first 'yoltools create' to copy config files to project (etc/yoptools.json).
Adapt these config files with correct values then launch again 'yoltools create' to continue the project creation.

yoltools sendToServer
Send current dir to server (rsync). 
If remote folder not yet exists, it's created. 
`;